// build script — tauri needs this or it throws a tantrum during compilation
// it does one thing. it does it fine. do NOT touch it. do NOT "improve" it. just leave it alone.
fn main() {
    tauri_build::build()
}
